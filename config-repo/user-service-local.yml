# local 프로파일: PC 개발 → 외부 도커 PostgreSQL
spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://192.168.55.223:5432/user_db
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
