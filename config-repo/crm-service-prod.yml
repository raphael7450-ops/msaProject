# prod 프로파일: 도커 서버 (homer 가 8083 사용 → 8084)
server:
  port: 8084

spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://user-db:5432/user_db
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}

app:
  base-path: /crm-service
